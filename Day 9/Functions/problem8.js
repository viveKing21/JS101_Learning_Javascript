// function sum(a,b){
//   console.log(a+b);
// }
// sum(1,2);


function sum(a,b){ // function declaration;
  
    return a+b; // adding the number  and returning
    
  }
  
  let output= sum(1,2);
  //13. we are declaring a variable output= sum(1,2);
  // we will call the function , it will go to line number 7;
  // a =1, b= 2;
  // compiler will go to line 9  and it sees return word, so it will return what is there after return word.
  // it will return a+b to the calling function 
  // output= 3;
  
  console.log(output);