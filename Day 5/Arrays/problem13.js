// find out how many strings are there in an array.

let a= [1,2,3,"hello", "h", "b",45, "hi", "jai", "masai"];

let count=0;
for(let i=0;i<a.length;i++){
  //1
  // if(a[i]*0 !=0){
  //   count++;
  // }
  
  // 2. ways
  // if(typeof(a[i]) != "number"){
  //   count++;
  // }
}

console.log(count);